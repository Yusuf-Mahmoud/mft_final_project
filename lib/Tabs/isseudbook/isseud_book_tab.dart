import 'package:flutter/material.dart';

class IsseudBook extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('isseud book Tab'),
      ),
    );
  }
}
